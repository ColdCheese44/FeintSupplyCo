npm run dashboard starts the Jarvis business dashboard server on port `4200`.

Open [http://localhost:4200](http://localhost:4200) in a browser after the server starts.

The dashboard auto-refreshes every 30 seconds and reads the local SQLite database in read-only mode.
